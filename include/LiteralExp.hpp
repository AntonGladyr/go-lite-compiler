#ifndef LITERAL_HPP
#define LITERAL_HPP

//#include <string>
//#include <Expression.hpp>

class LiteralExp { // : public Expression {
	public:
		LiteralExp() { }
        	/*bool valueBool;
		int valueInt;
		float valueFloat;
		std::string valueString;
		//TODO: runes
		//char valueRune;	
		
		virtual void accept(Visitor& v) override;
			
		~LiteralExp() { }
		LiteralExp(int _value, int _lineno) : valueInt(_value), Expression(_lineno) { }
		LiteralExp(float _value, int _lineno) : valueFloat(_value), Expression(_lineno) { }
		LiteralExp(char _value, int _lineno) : valueString(_value), Expression(_lineno) { }
        	LiteralExp(const std::string &_value, int _lineno) : valueBool(_value), Expression(_lineno) { }
		*/
};

#endif
